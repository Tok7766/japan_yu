<%= form_with(model:[post_image, post_image_comment]) do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-dark" %>
<% end %>